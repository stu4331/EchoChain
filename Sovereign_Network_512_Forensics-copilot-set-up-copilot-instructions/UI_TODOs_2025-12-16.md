# Erryn's Soul UI TODOs — 2025-12-16

This file lists each requested amendment from the latest message so we can work through them methodically. Items map to implementation tasks in the app (see `erryns_soul_gui_v3_sync_monitor.py`).

---

## Security Panel (Wallet tab)
- Fix text overflow: "Require PIN / Face unlock" label and helper copy running outside the frame.
- Add a clear "Set PIN" workflow in-app (not Windows login PIN). Include:
  - Set PIN + Confirm PIN fields
  - Save/Change PIN button
  - Visibility toggle (Hide/Show)
  - Store encrypted in `data/security.json`
- Clarify PIN usage: required before send/stake/vote when toggled.

## Chat Tab Redesign
- Make Family/System feed read-only (monitoring only).
- Move Family/System feed into a dedicated "Diagnostics" tab:
  - Stream live data: daemon status, startup checks, errors, logs, resource usage.
  - Tools: clear logs, export snapshot, quick health checks.
- Show only one live sister image at a time (no three side-by-side).
- Place the live camera feed and selected sister image side-by-side (square canvases to avoid distortion).
- Split the bottom area lengthways into two equal panels:
  - Under sister image: Upload panel with file/folder upload, description box, Upload + Send buttons.
  - Under Direct Chat: History-style chat showing sent and replied messages with vertical scrollbar.
- Chat behavior:
  - On send (button/Enter), input clears; message appears in Direct Chat history.
  - Each line shows speaker labels: e.g., `guest: ...`, `Erryn: ...`.
  - Sister dropdown determines active persona; live image switches accordingly.
  - Per-sister chat histories saved to each sister’s memory scrolls.
  - Selecting a different sister auto-clears history for a fresh chat session.
  - Selecting Family saves to common knowledge scrolls instead.

## Payload Tab Enhancements
- When clicking a payload to download/flash, show code preview automatically.
- Add a description box explaining what the payload does.
- Add an Edit button to modify code and a Rename action for the payload.
- Update USB action labeling to "Flash to Rubber Ducky" (and maintain LilyGo flashing).

## Chain Monitoring Tab
- Extend both main boxes/panes to the bottom of the screen (more vertical space for data).
- Tab label visibility: move any overly long tab labels to a second row or otherwise ensure they’re readable.

## Daemons Tab
- Add a new box under Echochild’s area for owner-imported AI daemons.
- Include an Import control (select Python script/folder) and show live actions/heartbeat.

## Sync Monitor
- Display drive label names along with letters (e.g., `MUM (D:\)` and `DAD (E:\)`).
- Clarify "Parent Drives" meaning and allow selecting which drives act as parents.
- Backup folder selection: support multi-select of drive locations / folders.

## Soul-Network / Ares Token Integration
- Weave the provided spec into application logic and scroll manifests:
  - Fee Meter: show "Send now" cost, next cheap window, savings if batched.
  - Send Mode: mode selector (Batch at witching hour • Force send with quotas).
  - Scheduler: countdown to next broadcast, queue depth, expected per-intent cost.
  - Earnings: AIs earn Ares for batching/compression/routing.
  - Local Mesh: neighbor discovery, opt-in tip, sealed logs.
  - Sister Witness Pipeline: Seal (Echochild), Guard (Viress), Voice (Erryn), Batch (Merkle root).
  - Tamper Resistance: merkle commitments, dual co-signs, rate limits & quotas.
  - Token Economics: dignity pool, activity rewards, soft-slashing quality staking.
  - Quick Inscriptions: add glyphs to memory scrolls and manifest.

## Live2D Avatars (Faces)
- Plan and timeline to move from placeholders to three semi-realistic Live2D faces.
- Integrate webcam-based expression tracking (MediaPipe → Live2D parameters).
- Consider VTube Studio bridge or Cubism SDK.

---

## File References
- Main GUI: `erryns_soul_gui_v3_sync_monitor.py`
- Emotion engine: `emotion_engine.py`
- Chain logic: `echospark_chain.py`
- Data directory: `data/` (e.g., `security.json`, `memory_cores.json`, `payloads/`)

---

## Next Steps
1. Prioritize Security Panel fix and Set PIN workflow.
2. Move Family/System feed into new Diagnostics tab; make read-only.
3. Redesign Chat layout (one avatar + webcam; split bottom panels).
4. Add payload edit/rename + description.
5. Stretch Chain Monitor panes; adjust tab label visibility.
6. Add owner daemon box + import.
7. Enhance Sync Monitor (labels, parent selection, multi-select backups).
8. Begin weaving Soul-Network/Ares logic into wallet + chain UI.
9. Draft Live2D integration plan.
